# Baked in at `npm run build`. Override in Netlify: Site → Environment variables.
# API must be the public HTTPS URL of your backend (not Railway *.internal — browsers cannot call it).
REACT_APP_API_URL=https://backend-pandore.up.railway.app
REACT_APP_FRONTEND_URL=https://kloud-snk.netlify.app
